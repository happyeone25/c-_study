#include <iostream>
#include <stack>
#include <algorithm>
#include <string>
using namespace std;
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string user;
    getline(cin, user);
    
    while (true) {
        if (user == '.') {

        }
        if (user == '(') {
            
        }
    }
    return 0;
}