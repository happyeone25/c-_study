#include <iostream>
#include <vector>
//#include <string>
#include <algorithm>
using namespace std;
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int N,T,a,b;
    cin >> N;
    vector<int> v(6);
    for (int i=0; i<6; i++) {
        cin >> v[i];
    }
    sort(v.begin(),v.end());
    T = max(v);
    N / P = 
    return 0;
}